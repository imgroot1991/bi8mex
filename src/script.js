if (document.readyState === "complete") {
}

/* AVG POSITION CALCULATOR
  var avgSize = 0;

  function avgPos(avgSize) {

    arrContracts = [1000, 500, 500];
    arrPrice = [6565, 6400, 6380];
    var totalCalculation = 0;

    totalContracts = arrContracts.reduce((x, y) => x + y, 0);


    var calculation = (a, b) => a.map((x, i) => [x, b[i]]);
    for (let [a, b] of calculation(arrContracts, arrPrice))
      totalCalculation += a / b;

    return avgSize = totalContracts / totalCalculation;

  }
  console.log(avgPos(avgSize));
 */

//code here
/*$(".filter").on("change", function() {
  if ($(this).val() == "10") {
    $("div").show();
  } else {
    $("div").hide();
    $("." + $(this).val()).show();
  }
});
//code
*/

// function myfunction() {
//   var opt = document.getElementById("mylaptop");

//   var mydevice = document.getElementById("mydevice");

//   mydevice.style.display = mylaptop.value == "Y" ? "block" : "none";
// }
